package com.example.internnepal.ui.theme

import androidx.compose.ui.graphics.Color

// --- Modern Neutral Palette ---

// Primary Brand Colors (Professional Blue)
val PrimaryBlue = Color(0xFF2563EB)
val PrimaryBlueDark = Color(0xFF1E40AF)
val PrimaryBlueLight = Color(0xFFDBEAFE)

// Neutral Backgrounds & Surfaces
val BackgroundLight = Color(0xFFF8FAFC)
val SurfaceWhite = Color(0xFFFFFFFF)
val SurfaceGray = Color(0xFFF1F5F9)

// Text Colors (Slate)
val TextMain = Color(0xFF0F172A)
val TextSecondary = Color(0xFF475569)
val TextMuted = Color(0xFF94A3B8)

// Semantic Colors
val SuccessGreen = Color(0xFF10B981)
val ErrorRed = Color(0xFFEF4444)
val WarningOrange = Color(0xFFF59E0B)

// Keeping old names mapped to new palette for compatibility where needed
val white = BackgroundLight
val pink = PrimaryBlue
val purple = PrimaryBlueLight
val orange = SurfaceGray
val grey = TextSecondary
val black = TextMain
val brown = PrimaryBlueDark
